# Compile and deploy sample

Install dependencies
```
npm install
```

Compile program
```
node compile Counter.sol
```

Deploy, invoke and call `Counter.sol` against `http://localhost:8545`:
```
node deploy
```

